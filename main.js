'use strict';

{
 
  const point = {
   x: 100,
   y: 180,
  };
 
//   const keys = Object.keys(point); //オブジェクト内のプロパティを列挙
//   keys.forEach(key => {
// 　　　console.log(`Key: ${key} Value: ${point[key]}`);

//   });

  const points = [
   {x:30, y:20},
   {x:10, y:50},
   {x:40, y:40},
  ];

  console.log(points[1].y);

}